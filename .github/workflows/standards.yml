# The checks live in Ninja6-MC/.github and are maintained in one place; this file is
# only the caller. Copied from that repo's templates/standards-stub.yml.
name: Standards

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  standards:
    uses: Ninja6-MC/.github/.github/workflows/standards.yml@70e00c7cd9f3064f494b3b9e7d65301d91c93c0c # main @ 2026-09-17
